discord:
  # Your Discord bot token.
  # Alternatively, set the DISCORD_TOKEN environment variable.
  token: ""

bitnet:
  # Path to the cloned bitnet.cpp source directory (contains build/bin/llama-cli)
  src_dir: "./bitnet_cpp_src"
  # Fast TinyDolphin/TinyLlama uncensored GGUF model
  model: "./models/tinydolphin/tinydolphin-2.8.1-1.1b-q4_k_m.gguf"
  # Number of CPU threads to use (Raspberry Pi 4 has 4 cores)
  threads: 4
  # Maximum context length in tokens. Lower is faster on Raspberry Pi.
  context: 2048
  # Sampling temperature (0.0 = deterministic, 1.0 = very random)
  temperature: 0.7
  # Nucleus sampling probability threshold
  top_p: 0.9
  # Top-K candidates to consider at each token step
  top_k: 40
  # Penalty for repeating recent tokens (1.0 = no penalty)
  repeat_penalty: 1.1
  # Maximum tokens to generate per response
  max_tokens: 512

database:
  # Path to the SQLite conversation memory database
  file: "memory.db"

logging:
  # Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
  level: "INFO"
